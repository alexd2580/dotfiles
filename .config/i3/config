# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1
set $win Mod4

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+Shift+Q kill

# start rofi (a program launcher)
bindsym $mod+d exec rofi -show run

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# change focus to the parent/child container
bindsym $mod+a focus parent
bindsym $mod+Shift+a focus child

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle
# shortcut reused for translator

# Move windows
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Move workspaces.
bindsym $mod+Control+Left move workspace to output left
bindsym $mod+Control+Right move workspace to output right

# split the current container
bindsym $mod+h split h
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# Move focused container to workspace.
bindsym $mod+Shift+1 move workspace 1
bindsym $mod+Shift+2 move workspace 2
bindsym $mod+Shift+3 move workspace 3
bindsym $mod+Shift+4 move workspace 4
bindsym $mod+Shift+5 move workspace 5
bindsym $mod+Shift+6 move workspace 6
bindsym $mod+Shift+7 move workspace 7
bindsym $mod+Shift+8 move workspace 8
bindsym $mod+Shift+9 move workspace 9
bindsym $mod+Shift+10 move workspace 10

# Reload the configuration file.
bindsym $mod+Shift+C reload
# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3).
bindsym $mod+Shift+R restart
# Exit i3 (logs you out of your X session).
bindsym $mod+Shift+E exit

# Resize window (you can also use the mouse for that).
mode "resize" {
    # These bindings resize the border in the direction you pressed.

    bindsym Shift+Left  resize shrink left  10 px or 10 ppt
    bindsym Left        resize grow   left  10 px or 10 ppt

    bindsym Shift+Down  resize shrink down  10 px or 10 ppt
    bindsym Down        resize grow   down  10 px or 10 ppt

    bindsym Shift+Up    resize shrink up    10 px or 10 ppt
    bindsym Up          resize grow   up    10 px or 10 ppt

    bindsym Shift+Right resize shrink right 10 px or 10 ppt
    bindsym Right       resize grow   right 10 px or 10 ppt

    # Back to normal: Enter or Escape.
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Disable title bars.
new_window pixel 3
hide_edge_borders smart

# Mouse settings.
focus_follows_mouse yes
mouse_warping none

# Focus and navigation settings.
workspace_auto_back_and_forth no
focus_on_window_activation smart
show_marks no

################################  DEFAULT   ####################################
################################ MY SECTION ####################################

# for_window [title="VLC"] floating enable

### Additional modes

# Currently not working.
# mode "Display-xRandR" {
#     bindsym $mod+s exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip same
#     bindsym $mod+o exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip off
#     bindsym $mod+a exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip auto
#     bindsym $mod+r exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip right
#     bindsym $mod+l exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip left
#     bindsym $mod+u exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip top
#     bindsym $mod+d exec --no-startup-id /home/sascha/.utils/xrandrUtil/xrandrFlip bottom
#     #bindsym $mod+b exec --no-startup-id feh --bg-fill /home/sascha/Pictures/Anime/code_compplex_better.jpg
#     bindsym $mod+b exec --no-startup-id feh --bg-fill ~/Pictures/Anime/background.jpg
#
#     bindsym XF86Display mode "default"
#     bindsym Escape mode "default"
# }
# bindsym XF86Display mode "Display-xRandR"

# This mode is used to unmap all i3 commands temporarily.
mode "UnMod" {
    bindsym $mod+q mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+q mode "UnMod"

# WALLPAPER
exec --no-startup-id feh --bg-fill ~/.config/i3/background.png

# SCREENSHOOTER Use the same naming scheme as the xfce4-screenshooter.
bindsym $mod+c exec --no-startup-id sleep 0.1 && import "Screenshot_$(date +%Y-%m-%d_%H-%M-%S.png)"

# AUDIO
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q sset Master 4%+ && killall -SIGUSR1 statorange
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q sset Master 4%- && killall -SIGUSR1 statorange
bindsym XF86AudioMute exec --no-startup-id amixer set Master toggle && killall -SIGUSR1 statorange

# BRIGHTNESS -> xbacklight required
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5% -time 1
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 5% -time 1

# TRANSLATOR
bindsym $mod+space exec --no-startup-id /home/sascha/.utils/translit/scripts/translate.sh
bindsym $mod+Delete exec --no-startup-id /home/sascha/.utils/translit/scripts/translate.sh selection

# TRAYER
# bindsym $mod+t exec /home/sascha/.utils/statorange/toggle_trayer.sh

### Startup jobs (move to unit files?)
exec --no-startup-id xcompmgr -cf -I 0.1 -O 0.1

bar {
    i3bar_command killall statorange; statorange
}

exec --no-startup-id i3lock -eln -i ~/.config/i3/background.png
